<p class="text-success m-0"><%= notice %></p>
<div class="p-2">
<h1><%=@user.name%> アカウント</h1>

<h2>投稿一覧</h2>
<%= render @user %>

<div>
<%if @user==current_user%>

<hr>

<div class="d-flex gap-2">
<%= link_to "ユーザー情報を編集", edit_user_path(@user), class:"btn btn-success"%> 
  <%= link_to "ユーザー削除", @user, data:{ turbo_method: :delete, turbo_confirm: "ほんとに削除しますか？"},  class:"btn btn-danger" %>
  </div>
  <%end%>
  <%=  link_to "みんなの投稿一覧に戻る", users_path %>
</div>
